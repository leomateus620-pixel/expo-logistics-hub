/** Photo-derived display dimensions shared by metadata and the lazy renderer.
 * Keep this contract independent of Three geometry construction. */
export const SOY_RESTROOM_PRESENTATION = { wallHeight: 0.78, roofRise: 0.19, visualHeight: 1.2 } as const;
