name: Territorial reconstruction
on:
  push:
    branches: [fix/test-drive-pecuaria-access]
  pull_request:
    paths:
      - 'src/features/commercial-map/**'
      - 'src/test/commercialMap*'
      - 'scripts/territorial*'
      - '.github/workflows/territorial-reconstruction.yml'
permissions:
  contents: read
concurrency:
  group: territorial-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true
jobs:
  validate:
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        project: [standard, wide, mobile]
    env:
      VITE_SUPABASE_URL: https://qa.invalid
      VITE_SUPABASE_PUBLISHABLE_KEY: qa-placeholder
      LIBGL_ALWAYS_SOFTWARE: '1'
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci
      - name: Map contracts against the original PR revision
        run: |
          mkdir -p territorial-validation
          git rev-parse HEAD > territorial-validation/commit.txt
          # The incoming PR already has unrelated failing map contracts. Keep
          # them visible and fail on every new or changed assertion failure.
          git worktree add --detach /tmp/rural-baseline 4f8bdf3777dc521d80ad67885278e79da7426e56
          ln -s "$GITHUB_WORKSPACE/node_modules" /tmp/rural-baseline/node_modules
          (cd /tmp/rural-baseline && npx vitest run src/test/commercialMap --maxWorkers=2 --testTimeout=60000 --reporter=json --outputFile="$GITHUB_WORKSPACE/territorial-validation/baseline-tests.json") || test -s territorial-validation/baseline-tests.json
          npx vitest run src/test/commercialMap --maxWorkers=2 --testTimeout=60000 --reporter=json --outputFile=territorial-validation/tests.json || test -s territorial-validation/tests.json
          python scripts/arena-compare-tests.py territorial-validation/baseline-tests.json territorial-validation/tests.json territorial-validation/comparison.json
      - name: TypeScript
        run: npm run typecheck
      - name: Production build
        run: |
          set -o pipefail
          npm run build 2>&1 | tee territorial-validation/build.log
      - name: Browser
        run: |
          python -m pip install playwright==1.57.0 Pillow==11.3.0
          python -m playwright install --with-deps chromium
      - name: Real-map views and metrics
        run: |
          npm run dev -- --host 127.0.0.1 --port 4188 > territorial-validation/server.log 2>&1 &
          for i in $(seq 1 45); do curl -fsS http://127.0.0.1:4188 > /dev/null && break; sleep 1; done
          python scripts/territorial-capture.py --project ${{ matrix.project }}
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: territorial-validation-${{ matrix.project }}-${{ github.sha }}
          path: territorial-validation/
          retention-days: 14
